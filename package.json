{
  "name": "react_frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "prestart": "node scripts/sync-db.cjs",
    "start": "react-scripts start",
    "predev": "node scripts/sync-db.cjs",
    "dev": "react-scripts start",
    "prebuild": "node scripts/sync-db.cjs",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "lint": "eslint src --ext .js,.jsx",
    "server": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "json-server": "^0.17.4",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router-dom": "^7.18.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
